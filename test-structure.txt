// Test file to verify JSX structure
const testStructure = `
case "text_block": {
  // ... content
  </div>
}
case "video": {
  // ... content
}
`;

console.log("Current structure around text_block case:");
console.log(testStructure);

// Check if this matches expected structure
const expectedStructure = `
case "text_block": {
  // ... content
  </div>
}
case "video": {
  // ... content
}
`;

console.log("Expected structure:");
console.log(expectedStructure);

// The issue might be missing closing tags or incorrect nesting
// Let's check the actual structure around the problem area
`;
